sources:
  - description: QUIQ-format table (output of quiq skill). Input data originates from MIMIC-IV clinical database.
    license: PhysioNet Credentialed Health Data License 1.5.0

created:
  by: Lee Sungmin (이성민)
  role: authoring
  date: 2026-06-10

reviews:
  - by: TBD
    date: TBD
    scope: technical-accuracy
    notes: >
      Verify that LLM pair identification correctly excludes sensitive variables
      (death_time, year_of_birth) and complex-context pairs. Confirm that
      same-table constraint and Primary_key merge logic matches original intent.

references:
  - LYDUS 품질관리 프로그램 활용 가이드라인 (비공개 내부 문서)
  - Original Python implementation: LYDUS_Sequence_Validity.py (이성민 작성)

changelog:
  - version: "1.0"
    date: 2026-06-10
    summary: >
      Initial skill creation. Python-only (no SQL — LLM pair identification required).
      Fixed SettingWithCopyWarning on combined_time_df slice assignments.
      Added try/except around ast.literal_eval LLM response parsing.
      Replaced growing pd.concat loop in _validate_sequence with list collection.
      Used os.path.join throughout. Added required=True to --config argparse.
